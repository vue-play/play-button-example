{
  "scripts": {
    "play": "poi --config play.config.js",
    "build:play": "poi build --config play.config.js",
    "deploy": "npm run build:play && gh-pages -d play/dist"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "poi": "^8.0.0",
    "vue-play": "^3.2.1"
  },
  "dependencies": {
    "bootstrap-vue": "^0.16.1"
  }
}
